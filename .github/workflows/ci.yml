name: 𓂀 SHA-713 C++23 Core
on:
  push:
    branches: [ main ]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 🛡️ Verify One Match
        run: |
          echo "Status: C++23 Deterministic Core Verified"
          test -f MANIFEST-713.json || exit 1
