name: SHA-713 CORE CI

on:
  push:
    branches: [ main ]

jobs:
  core:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Manifest guard
        run: |
          test -f MANIFEST-713.json || (echo "MISSING MANIFEST" && exit 1)
          echo "MANIFEST OK"

      - name: Repo sanity
        run: |
          echo "SYSTEM OK"
          echo "CI STABLE - SINGLE PIPELINE ACTIVE"
