name: SHA-713 Stable Core

on:
  push:
    branches: [ main ]

jobs:
  core:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Manifest validation
        run: |
          if [ ! -f MANIFEST-713.json ]; then
            echo "MANIFEST MISSING"
            exit 1
          fi

      - name: Repo health
        run: |
          echo "OK - SHA-713 STABLE CORE"
