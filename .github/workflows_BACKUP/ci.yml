name: SHA-713 Core Stability

on:
  push:
    branches: [ main ]

jobs:
  integrity:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Manifest check
        run: |
          echo "Checking MANIFEST-713.json"
          test -f MANIFEST-713.json

      - name: System OK
        run: |
          echo "SHA-713 OK - system stable"
